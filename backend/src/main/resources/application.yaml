spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://db_username:db_password@localhost:27017}
      database: pirullometro

youtube:
  api:
    channel-id: ${YOUTUBE_CHANNEL_ID:channel_id}
    base-url: https://www.googleapis.com
    key: ${YOUTUBE_API_KEY}
